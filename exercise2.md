# Dokumentation Übung 2

In dieser Übung wurden folgende Aufgaben erfolgreich umgesetzt:

* **Calculator:** Implementierung einer Calculator-Klasse für die Grundrechenarten inklusive Nulldivisions-Check.
* **Maven-Setup:** Konfiguration der `pom.xml` (Java 25, UTF-8, Compiler- und Exec-Plugin).
* **Logging mit Log4j2:** * Einbindung der Log4j-Abhängigkeit.
    * Konfiguration der `log4j2.xml`, sodass Logs parallel in der Konsole und in `logs/myapp.log` gespeichert werden.
    * Erstellung eines Git-sicheren Templates (`log4j2.xml.template`).
* **Git Workflow:** Nutzung von Feature-Branches und Merges.